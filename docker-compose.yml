services:
  frontend:
    build:
      context: ./poc-submodule-child-1
      dockerfile: Dockerfile
    volumes:
      - ./poc-submodule-child-1/app:/workspace/app
    ports:
      - 3000:5173
  backend:
    build:
      context: ./poc-submodule-child-2
      dockerfile: Dockerfile
    ports:
      - 3001:3001